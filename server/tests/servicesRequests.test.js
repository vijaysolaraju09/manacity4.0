describe('services.create', () => {
  test.todo('creates a service request');
});

describe('services.assign', () => {
  test.todo('assigns a provider to a service request');
});

describe('services.my', () => {
  test.todo('returns the current customer service requests');
});

describe('services.assigned', () => {
  test.todo('returns service requests assigned to the provider');
});
